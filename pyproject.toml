[project]
name = "dogo-seed-tools"
version = "0.1.0"
description = "Local seed collection and import helpers for the Dogo project."
requires-python = ">=3.10"
dependencies = [
    "PyMySQL==1.1.1",
]

[tool.uv]
package = false
